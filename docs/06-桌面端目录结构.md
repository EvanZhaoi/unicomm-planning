# UniComm Desktop 前端目录结构

> **UniComm 是企业级桌面协作应用，不是普通 Web 项目。**

## 一、项目结构

```
unicomm-desktop/                  # Tauri 2 + React 项目
├── src/                          # React 源码
│   │
│   ├── desktop/                   # 桌面能力层（Tauri 封装）
│   │   ├── user/                   # Windows 用户信息
│   │   │   ├── getCurrentWindowsUser.ts  # 获取当前 Windows 用户
│   │   │   └── index.ts
│   │   ├── device/                 # 设备信息
│   │   │   ├── DeviceService.ts    # 设备标识、OS 信息
│   │   │   └── index.ts
│   │   ├── tray/                 # 系统托盘
│   │   │   ├── TrayManager.tsx
│   │   │   ├── TrayMenu.tsx
│   │   │   └── index.ts
│   │   ├── window/               # 窗口管理
│   │   │   ├── WindowManager.ts
│   │   │   ├── useWindow.ts
│   │   │   └── index.ts
│   │   ├── notification/          # 系统通知
│   │   │   ├── notify.ts
│   │   │   └── index.ts
│   │   ├── filesystem/            # 本地文件系统
│   │   │   ├── FileService.ts
│   │   │   └── index.ts
│   │   ├── clipboard/            # 剪贴板
│   │   │   ├── ClipboardService.ts
│   │   │   └── index.ts
│   │   └── shortcut/             # 全局快捷键
│   │       ├── ShortcutManager.ts
│   │       └── index.ts
│   │
│   ├── native/                   # 原生能力抽象层
│   │   ├── api/                  # Tauri 命令调用封装
│   │   │   ├── tauri.ts         # Tauri 基础 API
│   │   │   └── invoke.ts        # invoke 封装
│   │   └── bridge/               # Rust <-> JS 桥接
│   │
│   ├── features/                 # 功能模块（按领域拆分）
│   │   │
│   │   ├── auth/                # 身份认证模块（桌面端）
│   │   │   ├── api/             # API 调用
│   │   │   │   └── verifyDesktopApi.ts  # 身份校验接口
│   │   │   ├── store/           # Zustand Store
│   │   │   │   └── authStore.ts  # 替换原 session.store
│   │   │   ├── components/      # 认证相关组件
│   │   │   │   ├── AuthStatusView.tsx  # 认证状态视图
│   │   │   │   └── AuthErrorView.tsx  # 认证错误视图
│   │   │   └── types/           # 类型定义
│   │   │       └── auth.types.ts
│   │   │
│   │   ├── memo/                 # Memo 功能模块
│   │   │   ├── api/             # API 调用
│   │   │   │   ├── memoApi.ts
│   │   │   │   ├── groupApi.ts
│   │   │   │   └── tagApi.ts
│   │   │   ├── store/           # Zustand Store（按领域）
│   │   │   │   ├── memoStore.ts
│   │   │   │   └── selectors.ts
│   │   │   ├── hooks/           # 业务 Hooks
│   │   │   │   ├── useMemos.ts
│   │   │   │   ├── useGroups.ts
│   │   │   │   └── useSearch.ts
│   │   │   ├── components/      # 功能内专用组件
│   │   │   │   ├── MemoEditor.tsx
│   │   │   │   ├── MemoList.tsx
│   │   │   │   ├── MemoItem.tsx
│   │   │   │   ├── GroupList.tsx
│   │   │   │   ├── TagSelector.tsx
│   │   │   │   └── MarkdownPreview.tsx
│   │   │   ├── types/          # 功能类型定义
│   │   │   │   └── memo.types.ts
│   │   │   └── utils/          # 功能工具函数
│   │   │       └── markdown.ts
│   │   │
│   │   ├── im/                  # IM 功能（未来）
│   │   ├── notify/              # Notify 功能（未来）
│   │   └── ai/                  # AI 功能（未来）
│   │
│   ├── stores/                   # 全局状态（按领域拆分）
│   │   ├── memo.store.ts        # Memo 状态
│   │   │                       # - memos[]
│   │   │                       # - currentMemo
│   │   │                       # - groups[]
│   │   │                       # - tags[]
│   │   ├── socket.store.ts      # WebSocket 连接状态
│   │   │                       # - connected
│   │   │                       # - reconnecting
│   │   │                       # - lastPing
│   │   ├── auth.store.ts       # 桌面端身份认证状态
│   │   │                       # - currentUser (DesktopUserInfo)
│   │   │                       # # - accessToken
│   │   │                       # - authStatus: checking|verified|rejected|offline
│   │   │                       # - verifyDesktopUser()
│   │   │                       # - clearSession()
│   │   └── settings.store.ts   # 应用设置
│   │                           # - theme
│   │                           # - notifications
│   │                           # - shortcuts
│   │
│   ├── components/               # 通用组件
│   │   ├── ui/                  # 基础 UI 组件（可抽离到 plugin）
│   │   │   ├── Button/
│   │   │   │   ├── Button.tsx
│   │   │   │   └── index.ts
│   │   │   ├── Input/
│   │   │   ├── Modal/
│   │   │   ├── Dropdown/
│   │   │   └── ...
│   │   │
│   │   ├── layout/              # 布局组件
│   │   │   ├── AppLayout/
│   │   │   ├── Sidebar/
│   │   │   ├── Header/
│   │   │   └── Footer/
│   │   │
│   │   └── business/            # 业务组件（可按需抽离）
│   │       ├── MemoEditor/
│   │       ├── MemoList/
│   │       ├── GroupTree/
│   │       └── TagSelector/
│   │
│   ├── pages/                   # 页面组件
│   │   ├── index.tsx           # 路由入口
│   │   ├── memo/
│   │   │   └── MemoPage.tsx   # Memo 主页面
│   │   └── settings/
│   │       └── SettingsPage.tsx
│   │
│   ├── services/                # 服务层
│   │   ├── request.ts          # Axios HTTP 请求封装
│   │   ├── socket.ts          # WebSocket 服务
│   │   │                     # - 连接管理
│   │   │                     # - 心跳检测
│   │   │                     # - 自动重连
│   │   ├── cache.ts           # 本地缓存服务
│   │   └── storage.ts         # 持久化存储
│   │
│   ├── types/                   # 全局类型定义
│   │   ├── api.ts             # API 响应类型
│   │   ├── user.ts            # 用户类型
│   │   └── global.d.ts        # 全局声明
│   │
│   ├── utils/                   # 工具函数
│   │   ├── format.ts
│   │   ├── debounce.ts
│   │   └── validation.ts
│   │
│   ├── styles/                  # 全局样式
│   │   ├── globals.css
│   │   └── variables.css
│   │
│   ├── App.tsx                 # 根组件
│   └── main.tsx                 # 入口
│
├── src-tauri/                    # Tauri/Rust 后端
│   ├── src/
│   │   ├── main.rs              # 入口
│   │   ├── lib.rs               # 库入口
│   │   ├── commands/            # Tauri 命令
│   │   │   ├── mod.rs
│   │   │   ├── user.rs          # Windows 用户命令
│   │   │   ├── device.rs        # 设备信息命令
│   │   │   ├── tray.rs          # 托盘命令
│   │   │   ├── window.rs        # 窗口命令
│   │   │   ├── notification.rs  # 通知命令
│   │   │   └── clipboard.rs     # 剪贴板命令
│   │   │
│   │   └── services/           # Rust 服务
│   │       ├── tray.rs         # 托盘服务
│   │       └── updater.rs      # 更新服务
│   │
│   ├── icons/                   # 应用图标
│   ├── tauri.conf.json         # Tauri 配置
│   ├── Cargo.toml               # Rust 依赖
│   └── capabilities/            # Tauri 权限配置
│
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── index.html
```

---

## 二、目录职责说明

### 2.1 按领域拆分 vs 按层级拆分

| 目录 | 说明 | 原则 |
|------|------|------|
| `stores/*` | 全局状态，按领域拆分 | 不要做页面级 store |
| `features/*` | 功能模块，按领域拆分 | 包含 API、Store、Hooks、组件 |
| `desktop/*` | 桌面能力，与 Tauri 交互 | 封装原生能力，供 features 调用 |
| `native/*` | 原生 API 抽象 | 提供统一的调用接口 |

### 2.2 components/ vs features/

| 目录 | 说明 | 可抽离到 Plugin |
|------|------|----------------|
| components/ui | 基础 UI 组件 | ✅ 是 |
| components/layout | 布局组件 | ✅ 部分 |
| components/business | 业务组件 | ⚠️ 视情况 |
| features/memo | 功能模块 | ❌ 否 |

---

## 三、状态管理规范

### 3.1 状态管理选择

| 场景 | 方案 | 说明 |
|------|------|------|
| 服务端数据 | TanStack Query | 缓存、同步、离线支持 |
| 客户端临时状态 | React useState | 不需要全局共享 |
| 全局共享状态 | Zustand | 按领域拆分 store |
| 表单状态 | React Hook Form | 复杂表单校验 |

### 3.2 Zustand Store 规范

**按领域拆分的 Store：**

```typescript
// stores/memo.store.ts
export const useMemoStore = create<MemoState>()((set, get) => ({
  memos: [],
  currentMemo: null,
  setCurrentMemo: (memo) => set({ currentMemo: memo }),
  // ...
}));

// stores/socket.store.ts
export const useSocketStore = create<SocketState>()((set) => ({
  connected: false,
  reconnecting: false,
  setConnected: (connected) => set({ connected }),
  // ...
}));

// stores/auth.store.ts
type AuthStatus = 'checking' | 'verified' | 'rejected' | 'offline';

interface DesktopUserInfo {
  username: string; // Windows 用户名
  employeeNo: string;
  displayName: string;
  departmentName: string;
  permissions: string[];
}

interface AuthState {
  currentUser: DesktopUserInfo | null;
  accessToken: string | null;
  authStatus: AuthStatus;
  verifyDesktopUser: (clientInfo: DesktopClientInfo) => Promise<boolean>;
  clearSession: () => void;
}

export const useAuthStore = create<AuthState>()((set, get) => ({
  currentUser: null,
  accessToken: null,
  authStatus: 'checking',
  
  verifyDesktopUser: async (clientInfo) => {
    set({ authStatus: 'checking' });
    try {
      const res = await api.post('/auth/desktop/verify', clientInfo);
      if (res.code === 200) {
        set({
          currentUser: res.data,
          accessToken: res.data.accessToken,
          authStatus: 'verified',
        });
        return true;
      } else {
        set({ authStatus: 'rejected' });
        return false;
      }
    } catch (e) {
      set({ authStatus: 'offline' });
      return false;
    }
  },
  
  clearSession: () => set({ currentUser: null, accessToken: null, authStatus: 'checking' }),
}));

// stores/settings.store.ts
export const useSettingsStore = create<SettingsState>()((set) => ({
  // 主题: system=跟随系统, light=亮色, dark=暗色
  theme: 'system',
  notifications: true,
  shortcuts: {},
  setTheme: (theme) => set({ theme }),
  setNotifications: (enabled) => set({ notifications: enabled }),
}));

interface SettingsState {
  theme: 'system' | 'light' | 'dark';
  notifications: boolean;
  shortcuts: Record<string, string>;
  setTheme: (theme: 'system' | 'light' | 'dark') => void;
  setNotifications: (enabled: boolean) => void;
}
```

**使用方式：**

```typescript
// 在组件中使用
const MemoList: React.FC = () => {
  const memos = useMemoStore((state) => state.memos);
  const setCurrentMemo = useMemoStore((state) => state.setCurrentMemo);
  // ...
};

// 选择器优化
const currentMemo = useMemoStore((state) => state.currentMemo);
```

---

## 四、组件分层架构

```
┌─────────────────────────────────────────────────────────┐
│  Pages（页面层）                                        │
│  职责：布局、路由跳转、错误边界                          │
├─────────────────────────────────────────────────────────┤
│  Features（功能层）                                     │
│  职责：业务逻辑、数据处理、API 调用                      │
├─────────────────────────────────────────────────────────┤
│  Components（组件层）                                    │
│  职责：UI 展示、可复用                                  │
├─────────────────────────────────────────────────────────┤
│  Desktop（桌面能力层）                                  │
│  职责：Tauri 封装、系统托盘、通知、文件系统              │
└─────────────────────────────────────────────────────────┘
```

---

## 五、桌面端能力接入

### 5.1 系统托盘

```typescript
// desktop/tray/TrayManager.ts
import { TrayIcon } from '@tauri-apps/api/tray';

export class TrayManager {
  async initialize() {
    const tray = await TrayIcon.new({
      tooltip: 'UniComm',
      menu: this.buildMenu(),
      onClick: () => this.showWindow(),
    });
  }

  private buildMenu() {
    return [
      { label: '显示窗口', onClick: () => this.showWindow() },
      { type: 'separator' },
      { label: '退出', onClick: () => this.quit() },
    ];
  }
}
```

### 5.2 系统通知

```typescript
// desktop/notification/notify.ts
import { sendNotification } from '@tauri-apps/plugin-notification';

export async function notify(title: string, body: string) {
  await sendNotification({ title, body });
}
```

### 5.3 WebSocket 服务

```typescript
// services/socket.ts
export class SocketService {
  private ws: WebSocket | null = null;

  connect() {
    this.ws = new WebSocket(WS_URL);
    this.ws.onopen = () => this.startHeartbeat();
    this.ws.onclose = () => this.reconnect();
  }

  onMessage(handler: (data: any) => void) {
    this.ws?.addEventListener('message', (e) => handler(JSON.parse(e.data)));
  }
}
```

---

## 六、组件抽离检查清单

当需要将组件抽离到 `unicomm-plugin` 时，检查：

- [ ] 不依赖特定业务数据
- [ ] 不依赖特定 API 路径
- [ ] Props 类型明确，无 any
- [ ] 无业务逻辑，所有逻辑通过 Props 传入
- [ ] 无副作用
- [ ] 样式可自定义

### 6.1 业务组件抽离说明

以下组件当前放在 `unicomm-desktop` 中，未来可能抽离：

- `MemoEditor` - Markdown 编辑器
- `MemoList` - Memo 列表组件
- `GroupList` - 分组列表组件
- `TagSelector` - 标签选择器

**抽离方式：通过 Props + API Adapter 模式**

```
┌─────────────────────────────────────┐
│  unicomm-plugin                      │
│  - MemoEditor (纯展示)              │
│  - Props: content, onChange         │
│  - 无直接 API 调用                   │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│  API Adapter (features/memo 层)      │
│  - getMemos, createMemo, etc.      │
└─────────────────────────────────────┘
```
