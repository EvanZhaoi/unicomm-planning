# UniComm 项目结构设计

## 一、项目定位

**UniComm 是企业级桌面协作应用，不是普通 Web 管理后台。**

核心特点：
- 长期运行的后台应用
- 系统托盘常驻
- WebSocket 保持连接
- 支持离线工作
- 本地数据缓存
- 桌面原生能力

---

## 二、项目拆分策略

### 2.1 仓库拆分原则

| 仓库 | 类型 | 说明 |
|------|------|------|
| unicomm-planning | 文档仓库 | 项目规划、需求文档、技术方案（当前） |
| unicomm-server | 后端服务 | Spring Boot 后端 API |
| unicomm-desktop | 桌面应用 | Tauri 2 + React |
| unicomm-plugin | 插件系统 | 未来：可嵌入第三方系统的 SDK |

### 2.2 系统边界

```
┌─────────────────────────────────────────────────────────────┐
│                      UniComm 企业级桌面协作应用                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────────────────────────────────────┐              │
│   │           unicomm-desktop                 │              │
│   │   (Tauri 2 + React + 本地能力)            │              │
│   │                                            │              │
│   │   ┌────────────────────────────────────┐ │              │
│   │   │          桌面能力层                  │ │              │
│   │   │  托盘 / 窗口 / 通知 / 文件系统       │ │              │
│   │   └────────────────────────────────────┘ │              │
│   │   ┌────────────────────────────────────┐ │              │
│   │   │          React UI 层                │ │              │
│   │   │  Memo / IM / Notify / AI           │ │              │
│   │   └────────────────────────────────────┘ │              │
│   │   ┌────────────────────────────────────┐ │              │
│   │   │          本地数据层                  │ │              │
│   │   │  状态管理 / 离线缓存 / 本地存储       │ │              │
│   │   └────────────────────────────────────┘ │              │
│   └──────────────────────────────────────────┘              │
│                         │                                    │
│                         │ WebSocket                          │
│                         ▼                                    │
│   ┌──────────────────────────────────────────┐              │
│   │         unicomm-server (后端)              │              │
│   │         Spring Boot + MySQL               │              │
│   └──────────────────────────────────────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、Desktop 项目结构（unicomm-desktop）

### 3.1 目录结构

```
unicomm-desktop/
├── src/                          # React 源码
│   │
│   ├── desktop/                  # 桌面能力层（Tauri 封装）
│   │   ├── tray/                 # 系统托盘
│   │   ├── window/               # 窗口管理
│   │   ├── updater/              # 自动更新
│   │   ├── notification/          # 系统通知
│   │   ├── filesystem/            # 本地文件系统
│   │   ├── clipboard/             # 剪贴板
│   │   └── shortcut/              # 全局快捷键
│   │
│   ├── native/                   # 原生能力抽象层
│   │   ├── api/                  # Tauri 命令封装
│   │   └── bridge/               # Rust <-> JS 桥接
│   │
│   ├── features/                 # 功能模块（按领域拆分）
│   │   ├── memo/                 # Memo 模块
│   │   │   ├── api/             # API 调用
│   │   │   ├── store/           # Zustand Store
│   │   │   ├── hooks/           # 业务 Hooks
│   │   │   ├── components/       # 功能内专用组件（MemoWorkspace、QuickMemoWindow 等）
│   │   │   └── types/           # 类型定义
│   │   ├── im/                   # IM 模块（未来）
│   │   ├── notify/               # Notify 模块（未来）
│   │   └── ai/                   # AI 模块（未来）
│   │
│   ├── stores/                   # 全局状态（按领域拆分）
│   │   ├── memo.store.ts         # Memo 状态
│   │   ├── socket.store.ts        # WebSocket 连接状态
│   │   ├── session.store.ts       # 用户会话状态
│   │   └── settings.store.ts      # 应用设置
│   │
│   ├── components/               # 通用组件
│   │   ├── ui/                  # 基础 UI（Button、Input 等）
│   │   ├── layout/               # 布局组件
│   │   └── business/             # 业务组件（可按需抽离）
│   │
│   ├── pages/                   # 页面组件
│   │   ├── memo/                 # Memo 页面
│   │   ├── settings/             # 设置页面
│   │   └── ...
│   │
│   ├── services/                # 服务层
│   │   ├── request.ts           # HTTP 请求封装
│   │   ├── socket.ts            # WebSocket 服务
│   │   └── cache.ts             # 本地缓存服务
│   │
│   ├── types/                   # 全局类型
│   │
│   ├── styles/                  # 全局样式
│   │
│   ├── App.tsx                  # 根组件
│   └── main.tsx                 # 入口
│
├── src-tauri/                    # Tauri/Rust 后端
│   ├── src/
│   │   ├── main.rs              # 入口
│   │   ├── lib.rs               # 库入口
│   │   ├── commands/             # Tauri 命令
│   │   │   ├── mod.rs
│   │   │   ├── tray.rs          # 托盘命令
│   │   │   ├── window.rs         # 窗口命令
│   │   │   ├── notification.rs   # 通知命令
│   │   │   ├── filesystem.rs     # 文件系统命令
│   │   │   └── clipboard.rs      # 剪贴板命令
│   │   │
│   │   ├── services/            # Rust 服务
│   │   │   ├── tray.rs          # 托盘服务
│   │   │   └── updater.rs       # 更新服务
│   │   │
│   │   └── config/             # 配置
│   │
│   ├── icons/                   # 应用图标
│   ├── tauri.conf.json         # Tauri 配置
│   └── Cargo.toml               # Rust 依赖
│
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── index.html
```

### 3.2 状态管理结构

```
stores/
├── memo.store.ts        # Memo 模块状态
│   ├── memos[]         # Memo 列表
│   ├── currentMemo     # 当前编辑的 Memo
│   └── groups[]        # 分组列表
│
├── socket.store.ts     # WebSocket 连接状态
│   ├── connected       # 是否连接
│   ├── reconnecting    # 是否重连中
│   └── lastPing        # 最后心跳时间
│
├── session.store.ts    # 用户会话
│   ├── user            # 当前用户
│   ├── token           # 认证 token
│   └── expiresAt       # 过期时间
│
└── settings.store.ts   # 应用设置
    ├── theme            # 主题
    ├── notifications    # 通知设置
    └── shortcuts        # 快捷键配置
```

---

## 四、Server 项目结构（unicomm-server）

### 4.1 模块划分

```
src/main/java/com/unicomm/
├── UniCommApplication.java   # 启动入口
├── common/                   # 统一返回、错误码、异常、分页
├── config/                   # CORS、数据源、OpenAPI、Sa-Token、WebSocket 配置
└── module/                   # 业务模块
    ├── auth/                 # 桌面认证、设备信任、人员适配器、审计
    ├── member/               # 人员搜索
    └── memo/                 # Memo、分组、相关人、收藏、置顶、实时事件
```

### 4.2 目录规范

| 目录 | 职责 | 规范 |
|------|------|------|
| controller | 接收请求、参数校验、调用 service | 禁止写业务逻辑 |
| service | 业务逻辑处理、事务管理 | 调用 mapper，不直接写数据库访问代码 |
| repository | 数据库访问封装（后续需要时增加） | 禁止写业务逻辑 |
| entity | 数据库实体或查询承载对象（后续按需增加） | 禁止添加业务字段 |
| dto | 数据传输对象，请求参数 | 用于接收前端数据 |
| vo | 视图对象，响应数据 | 用于返回给前端 |
| config | 配置类 | 按功能划分 |

> 当前后端仍是单体仓库内的模块化包结构，不拆 Maven 多模块。数据库访问已统一到 MyBatis-Plus Mapper：简单实体使用 BaseMapper，复杂权限、分页和聚合查询保留在 Mapper 显式 SQL 中，Service 不再直接执行数据库访问。

---

## 五、桌面端能力清单

### 5.1 基础能力

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 系统托盘 | 最小化到托盘、托盘菜单、托盘通知 | P0 |
| 后台运行 | 关闭窗口隐藏到后台，托盘仍常驻 | P0 |
| 多窗口 | 主窗口、快速 Memo 窗口、设置入口 | P0 |
| 窗口管理 | 置顶、最小化、最大化、关闭 | P0 |
| 自动更新 | 检测更新、下载更新、提示重启 | P2 |
| 全局快捷键 | 自定义快捷键触发主界面和快速 Memo | P0 |
| 快捷键设置 | 设置页修改快捷键并立即生效 | P0 |

### 5.2 通信能力

| 能力 | 说明 | 优先级 |
|------|------|--------|
| WebSocket | 与后端保持长连接、消息推送 | P0 |
| 重连机制 | 网络断开自动重连 | P0 |
| 心跳检测 | 检测连接状态 | P0 |

### 5.3 本地能力

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 本地缓存 | 热点数据本地缓存、离线支持 | P0 |
| 本地数据库 | SQLite 预留（未来离线扩展） | P2 |
| 系统通知 | 桌面通知、静默通知 | P1 |
| 剪贴板 | 读取/写入剪贴板 | P1 |
| 文件拖拽 | 拖拽文件到应用 | P2 |
| 本地文件系统 | 读取/保存本地文件 | P2 |

### 5.4 离线能力

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 离线检测 | 检测网络状态 | P0 |
| 离线操作 | 离线时操作入队、联网后同步 | P1 |
| 冲突处理 | 离线编辑与在线数据冲突解决 | P2 |

---

## 六、未来扩展预留

### 6.1 多租户预留

```sql
-- owner_username 字段用于数据隔离
-- 未来可扩展 tenant_id 字段支持多租户
```

### 6.2 插件化预留

```sql
-- 模块标识字段
module_code VARCHAR(32)  -- MEMO, IM, NOTIFY, AI
```

### 6.3 消息通道预留

```sql
-- IM 模块需要用到
channel_type VARCHAR(32)  -- PRIVATE, GROUP, BROADCAST
```
